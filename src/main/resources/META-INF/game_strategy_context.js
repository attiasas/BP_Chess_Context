importPackage(Packages.chess);

var autoMovesBlack = true;
var autoMovesWhite = true;

function setStrategies()
{
    if(autoMovesBlack) setBlackStrategy();
}

setStrategies();

function setBlackStrategy() 
{
    
}